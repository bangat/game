{
  "name": "minigame-sky-tower",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node tools/serve.cjs",
    "build": "node tools/validate.cjs",
    "test": "node tests/multiplayer.cjs",
    "emulators": "firebase emulators:start --config firebase.emulator.json --project demo-sky-tower --only auth,database"
  },
  "devDependencies": { "playwright": "1.60.0", "firebase-tools": "14.16.0" }
}
